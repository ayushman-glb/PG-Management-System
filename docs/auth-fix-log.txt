================================================================================
ROOMBAE — AUTHENTICATION & SECURITY FIX LOG (PERMANENT RECORD)
================================================================================
Generated: 2026-08-19
Target Systems: React 18 Frontend, Express 4 + TypeScript Backend, MongoDB Atlas (Prisma 6), Redis Cache

--------------------------------------------------------------------------------
SECTION 1 — REFRESH-TOKEN COOKIE SAMESITE / CROSS-SITE FIX
--------------------------------------------------------------------------------
Diagnosis:
- Backend res.cookie("refreshToken") in backend/src/modules/auth/auth.controller.ts:
  * httpOnly: true
  * secure: env.NODE_ENV === "production"
  * sameSite: env.NODE_ENV === "production" ? "none" : "lax"
  * domain: undefined (host-only)
  * path: was undefined (now explicitly set to "/")
  * maxAge: 7 days (or 30 days on rememberMe)
- CORS in backend/src/app.ts:
  * Allowed origins include: "https://ayushman-glb.github.io", "https://pg-management-system-boxb.onrender.com", localhost:5173, etc.
  * Wildcard "*" is NOT used.
  * credentials: true is explicitly enabled.
- Frontend HTTP Clients:
  * backend fetch calls in frontend/src/services/api.ts and frontend/src/services/auth.service.ts specify credentials: "include".
  * Render trust proxy: app.set("trust proxy", 1) is present.

Fix:
- Standardized all refreshToken cookie calls using getRefreshTokenCookieOptions with explicit path: "/", httpOnly: true, secure: true (in production), sameSite: "none" (in production).

--------------------------------------------------------------------------------
SECTION 2 — LOGOUT CLEAR-COOKIE ATTRIBUTE ALIGNMENT
--------------------------------------------------------------------------------
Diagnosis:
- In logout handler (backend/src/modules/auth/auth.controller.ts), res.clearCookie("refreshToken") was missing explicit path: "/".

Fix:
- Updated logout handler to pass exact matching attributes: { httpOnly: true, secure: isProduction, sameSite: isProduction ? "none" : "lax", path: "/" }, ensuring the browser permanently removes the cookie.

--------------------------------------------------------------------------------
SECTION 3 — EXCLUSION OF BANK & FINANCIAL IDENTIFIERS FROM LOCALSTORAGE DRAFT
--------------------------------------------------------------------------------
Diagnosis:
- In frontend/src/features/auth/pages/Auth.tsx, the autosave draft previously stored accountHolderName, bankName, ifscCode, accountNumber, and upiId in browser localStorage under "roombae_incomplete_signup".

Fix:
- Modified the autosave useEffect to strictly exclude all financial identifiers (accountHolderName, bankName, ifscCode, accountNumber, confirmAccountNumber, upiId) from localStorage persistence.
- Updated resumeIncompleteSignup to keep bank fields blank and prompt the user to re-enter financial credentials when resuming Step 3.

--------------------------------------------------------------------------------
SECTION 4 — OWNER KYC STATUS REVIEW & APPROVAL GATE
--------------------------------------------------------------------------------
Diagnosis:
- The OwnerKYC model existed with verificationStatus: OwnerKYCStatus (PENDING, VERIFIED, REJECTED, EXPIRED), but User model had verificationStatus: "VERIFIED" without a dedicated kycStatus gate.
- New Owner registrations did not enforce a gate before property creation.

Fix:
- Added kycStatus String @default("PENDING_REVIEW") to the User model in backend/prisma/schema.prisma and regenerated Prisma client.
- Implemented requireKycApproved middleware in backend/src/middleware/authMiddleware.ts.
- Gated property listing creation (POST /api/v1/properties/) with requireKycApproved, returning 403 KYC_PENDING_REVIEW for unverified owners while keeping login and dashboard navigation active.

--------------------------------------------------------------------------------
SECTION 5 — CANONICAL LOWERCASE EMAIL NORMALIZATION
--------------------------------------------------------------------------------
Diagnosis:
- AuthRepository.findByIdentifier used multiple OR clauses with mode: "insensitive" regex filters that bypassed MongoDB's exact @unique index.

Fix:
- Enforced canonical lowercase normalization (.trim().toLowerCase()) at registration, user creation, Google OAuth linking, and login identifier lookups.
- Optimized findByIdentifier and findByEmail to execute exact point lookups on { email: lower }.
- Created standalone read-only diagnostic script at backend/scripts/diagnoseEmailCaseCollisions.ts to scan for any legacy casing collisions without mutating data.

--------------------------------------------------------------------------------
SECTION 6 — xss-clean REMOVAL & HTML OUTPUT ESCAPING
--------------------------------------------------------------------------------
Diagnosis:
- xss-clean was previously uninstalled from middleware in app.ts; remaining security stack uses Helmet, HPP, express-mongo-sanitize, and rate limiting.

Fix:
- Implemented escapeHtml output sanitization in backend/src/modules/email/email.templates.ts to safely escape user-supplied strings (names, inputs) before interpolation into HTML email templates.
- Confirmed Helmet Content-Security-Policy is active with script-src 'self'.

--------------------------------------------------------------------------------
SECTION 7 — OTP ATTEMPT LOCKOUT COUNTER SYNCHRONIZATION (REDIS & MONGODB)
--------------------------------------------------------------------------------
Diagnosis:
- In backend/src/infrastructure/otp/RedisOtpService.ts, OTPs and attempt counters were stored only in Redis when online. If Redis failed over to MongoDB, attempt counters were reset to zero.

Fix:
- Established MongoDB OtpToken as the authoritative persistence layer with dual-write to Redis for fast-path caching.
- Synchronized failed attempt increments across both Redis (attemptsKey) and MongoDB (OtpToken.attempts).
- Checked attempt counts against MAX_ATTEMPTS (5) across both Redis and MongoDB before allowing verification.

--------------------------------------------------------------------------------
SECTION 8 — REFRESH TOKEN REUSE DETECTION & SECURITY AUDIT LOGGING
--------------------------------------------------------------------------------
Diagnosis:
- Refresh token reuse detection was implemented in auth.service.ts (revoking active sessions on reuse of rotated token), but did not persist a SecurityAuditEvent log.

Fix:
- Added SecurityAuditEvent creation in backend/src/modules/auth/auth.service.ts upon detecting revoked token presentation (eventType: "SESSION_REVOKED", severity: "CRITICAL", riskScore: 90, riskLevel: "HIGH").

--------------------------------------------------------------------------------
SECTION 9 — FRONTEND CONCURRENT 401 SILENT REFRESH DEDUPLICATION
--------------------------------------------------------------------------------
Diagnosis:
- In frontend/src/services/auth.service.ts, AuthService.request executed direct raw fetch on 401 without utilizing the shared refreshPromise singleton, allowing multiple concurrent 401s to fire redundant refresh requests.

Fix:
- Centralized all 401 token refresh handling through a single in-flight Promise (this.refreshPromise).
- All simultaneous 401 requests await the exact same singleton refresh call; upon resolution, all pending requests retry transparently with the new access token.

--------------------------------------------------------------------------------
SECTION 10 — CDN EDGE-CACHE PROTECTION FOR AUTHENTICATED RESPONSES
--------------------------------------------------------------------------------
Diagnosis:
- Authenticated endpoints lacked explicit Cache-Control headers, presenting a risk of edge CDN caching across multi-tenant requests.

Fix:
- Added res.setHeader("Cache-Control", "private, no-store") inside authenticate middleware in backend/src/middleware/authMiddleware.ts.
- Public non-authenticated endpoints remain unaffected.

--------------------------------------------------------------------------------
SECTION 11 — API RESPONSE ENVELOPE SHAPE VALIDATION
--------------------------------------------------------------------------------
Diagnosis:
- Verified backend controllers (login, register, refreshToken, logout, me) consistently return:
  { success: true, message: string, data: { user, accessToken, ... } }
- Verified frontend consumers (AuthService, ApiClient) read response.data.accessToken, response.data.user, etc.

Result:
- Frontend and backend contracts are 100% aligned.

================================================================================
END OF LOG
================================================================================
